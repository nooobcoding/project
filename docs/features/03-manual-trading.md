# 03. 수동 매매

**상태**: 구현중 (코드 작성 완료, 로컬 DB 마이그레이션·수동 검증 대기)
**화면 ID**: SCR-03
**의존성**: `01-auth` (초기 시드머니로 즉시 매매 가능). 지정가 주문의 체결은 [09-execution-engine.md](09-execution-engine.md)의 체결 엔진을 이 기능과 함께 구현한다.
**연관 문서**: [00-overview.md](../00-overview.md) 5장(데이터 모델 확정 지점), 6장 원칙 4(코인 단위 잠금), [09-execution-engine.md](09-execution-engine.md)

---

## 1. 목적

코인 조회·차트·호가 확인 및 지정가/시장가/예약가 주문. **여기서 확정되는 `orders`/`holdings` 데이터 모델과 주문 실행 로직은 07(자동매매)이 그대로 재사용**하므로, 이 기능의 설계 품질이 이후 자동매매 구현 난이도를 좌우한다.

레이아웃(업비트 거래소 화면과 동일한 배치): 좌측 넓은 열(위: 시세·차트, 아래: 호가창 220px + 주문 패널) + 우측 코인 목록(260px, 두 행에 걸쳐 세로로 긴 패널) — 코인 목록은 검색창이 상단에 고정되고 목록만 스크롤된다. 거래내역·미체결 주문 목록은 주문 패널과 같은 폭으로 그 바로 아래에 세로로 쌓인다.

---

## 2. 화면 구성

### 2-A. 좌측 상단 — 시세·차트

| 컴포넌트 | 유형 | 설명 |
|---|---|---|
| 현재가 대형 텍스트 | Price Display | 등락률 색상 표시 |
| 고가·저가·거래량 | Info Row | 당일 고가/저가, 24H 거래량 |
| 시간대 탭 | Tab Group | 1분/10분/30분/1시간/일 |
| 캔들 차트 | Candlestick Chart | OHLCV 툴팁 |

### 2-B. 우측 — 코인 목록 (좌측 열 전체 높이만큼 세로로 긴 패널)

| 컴포넌트 | 유형 | 설명 |
|---|---|---|
| 코인 검색 박스 | Input (search) | 코인명/심볼 실시간 필터링. 패널 상단 고정, 목록 스크롤과 무관하게 항상 노출 |
| 코인 목록 행 | List Item | 코인명(심볼)+현재가+등락률. 선택 행 배경 강조. **자동매매 운용 중인 코인은 "자동" 배지 표시** (07 완료 후 연동) |

### 2-C. 좌측 하단(좌) — 호가창

매도 호가 10단(파랑) / 현재가 / 매수 호가 10단(빨강). 클릭 시 주문가격 자동 입력.

### 2-D. 좌측 하단(우) — 주문 패널 · 잔고 (호가창 옆)

| 컴포넌트 | 유형 | 설명 |
|---|---|---|
| 매수/매도 탭 | Tab | 매수(빨강)/매도(파랑) |
| 주문 유형 탭 | Tab | 지정가/시장가/예약가 (시장가 시 가격 입력 비활성) |
| 감시가격 입력 (신규) | Input (number) | **예약가 선택 시에만 노출**. 업비트식 예약가 주문 — 현재가가 감시가격에 도달하면 주문가격으로 지정가 주문이 자동 생성된다 |
| 주문가격/수량 입력 | Input (number) | 콤마 포매팅 |
| 비율 버튼 | Button Group | 10/25/50/75/최대 |
| 주문금액 자동계산 | Read-only | 가격×수량 |
| 매수/매도 버튼 | Button (Primary) | 잔고 부족 시 비활성화 |
| **잠금 배너** (신규) | Alert Banner | 선택 코인이 자동매매 운용 중이면 주문 영역 전체 비활성화 + "이 코인은 자동매매(전략명)가 운용 중입니다. [자동매매 화면으로 이동]" 표시. 07 완료 전까지는 항상 숨김 상태로 유지 |
| 보유 원화/코인 표시 | Info Row | 실시간 |

### 2-E. 주문 패널 아래 — 거래내역 (주문 패널과 같은 폭)

체결·취소된 최근 주문 목록(매수/매도 배지, 코인, 가격, 수량, 상태, 시각). 현재 선택된 코인 기준으로 조회하며 취소 버튼은 없다(과거 내역이므로 2-F 미체결 목록과 구분).

### 2-F. 거래내역 아래 — 미체결 주문 목록 (주문 패널과 같은 폭)

| 컴포넌트 | 유형 | 설명 |
|---|---|---|
| 미체결 주문 목록 | List | 취소 버튼 (확인 없이 즉시 처리) |

---

## 3. 인터랙션

| 위치 | 이벤트 | 처리 | 결과 |
|---|---|---|---|
| 코인 목록 행 클릭 | Click | 전체 패널 데이터 갱신 | Spinner 후 표시 |
| 호가 행 클릭 | Click | 주문가격 자동 입력 | 주문금액 재계산 |
| 비율 버튼 클릭 | Click | 잔고 기준 수량 자동 계산 | - |
| 주문 버튼 클릭 | Click | 잔고 검증 → 주문 생성 → 미체결 추가 | 성공 토스트 / 실패 메시지 |
| 미체결 취소 버튼 | Click | 즉시 취소 → 동결 잔고 반환 | 잔고 갱신 |

---

## 4. 오류 처리

| 상황 | 메시지 |
|---|---|
| 잔고 부족(매수) | 보유 원화가 부족합니다. 입출금 화면에서 충전 후 이용해주세요. |
| 잔고 부족(매도) | 보유 [코인명] 수량이 부족합니다. |
| 가격/수량 0 이하 | 올바르게 입력해주세요. |
| API 실패 | 주문 처리 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요. |

---

## 5. 데이터 모델 (확정 — 이후 기능 공유)

> 전체 스키마의 기준은 [01-erd.md](../01-erd.md)다. 아래는 이 기능 관점의 발췌.

```
orders
  id (PK)
  user_id (FK)
  coin_symbol
  side               -- buy | sell
  order_type         -- limit | market | reserved  (reserved는 감시가격 도달 시 limit으로 승격만 됨, 01-erd.md 3.7절)
  price              -- market 주문은 체결가 기록, reserved는 승격 후 사용할 주문가격
  quantity
  status             -- pending | filled | canceled  (전이는 09-execution-engine.md 체결 엔진이 담당)
  source             -- manual | auto   (07에서 auto 사용)
  strategy_slot_id   -- FK, nullable (07에서 사용)
  realized_profit    -- nullable, sell 체결 시 holdings.avg_buy_price 기준 손익 기록 (08 월별 수익 리포트용)
  fee                -- 체결 수수료(원화), pending 동안 0. 계산 규칙 01-erd.md 3.2절
  trigger_price      -- nullable, 예약가 주문의 감시가격 (01-erd.md 3.7절)
  trigger_direction  -- nullable, rising | falling — 생성 시점 1회 확정
  created_at
  filled_at

holdings
  user_id (FK)
  coin_symbol
  quantity
  avg_buy_price
  PRIMARY KEY (user_id, coin_symbol)
```

---

## 6. API 엔드포인트 (초안)

| Method | Path | 설명 |
|---|---|---|
| GET | `/api/coins` | 코인 목록 + 현재가/등락률 |
| GET | `/api/coins/{symbol}/balance` | (구현 시 추가) 가용 원화/가용 코인 수량 — 01-erd.md 3.1절 파생 계산을 프론트에서 중복 구현하지 않기 위해 초안에 없던 엔드포인트를 추가 |
| GET | `/api/coins/{symbol}/candles?interval=` | 캔들 데이터 |
| WS | `/ws/orderbook/{symbol}` | 호가 실시간 구독. 데이터량이 커서 전체 상시 구독 대상(00-overview 3장의 시세 캐시)에 포함하지 않고, 사용자가 이 화면에서 보고 있는 심볼만 온디맨드로 Upbit에 구독 요청 — 화면을 벗어나거나 다른 코인을 선택하면 해제 |
| POST | `/api/orders` | 주문 생성 (지정가/시장가/예약가 공통 — 07 자동매매도 이 로직을 내부 호출로 재사용). 시장가는 즉시, 지정가/예약가는 `pending`으로 저장 후 [09-execution-engine.md](09-execution-engine.md) 체결 엔진이 체결(예약가는 감시가격 도달 시 지정가로 승격 후 체결) |
| DELETE | `/api/orders/{id}` | 미체결 취소 |
| GET | `/api/orders?status=pending` | 미체결 목록 |
| GET | `/api/orders?status=history&coin_symbol=` | (구현 시 추가) 거래내역 — 체결·취소된 최근 주문(기본 50건), 코인 필터 선택 |
| GET | `/api/coins/{symbol}/lock-status` | (07 완료 후 추가) 자동매매 잠금 여부 |

---

## 7. 관련 요구사항 매핑

| FR ID | 내용 |
|---|---|
| FR-M01~M09 | v1.0과 동일 (코인목록/시세차트/호가창/지정가·시장가주문/비율버튼/주문금액계산/미체결관리/잔고표시) |
| FR-M10 (신규) | 자동매매 운용 코인 수동주문 잠금 — **07-auto-trading 구현 시 함께 연동, 그 전까지는 UI 훅만 존재**. 07의 다중 슬롯 잔고 검증(슬롯 활성화 시점 + 실행 시점 재검증)도 이 잠금과 같은 목적(잔고·포지션 충돌 방지)으로 함께 동작한다 |
| FR-M11 (신규) | 예약가(스탑) 주문 — 감시가격 도달 시 지정가 주문 자동 생성. 01-erd.md 3.7절 |
| FR-M12 (신규) | 거래내역 조회 — 체결·취소된 과거 주문 목록 |
